# {{DISPLAY_NAME}}

Codex plugin scaffold.

## What this plugin should contain

- a `.codex-plugin/plugin.json` manifest
- one or more skills under `skills/`
- optional helper scripts under `scripts/`
- any plugin-specific assets under `assets/`

## Next steps

1. Replace the manifest placeholders in `.codex-plugin/plugin.json`
2. Add the real workflow skill in `skills/`
3. Add scripts only when the plugin needs local automation or validation
